<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Andrea Guijarro | Digital Marketing & Creative Strategy</title>
  <style>
    :root {
      --bg-color: #f7f7f7;
      --card-bg: #ffffff;
      --text-main: #111111;
      --text-muted: #555555;
      --border-color: #111111;
      --btn-bg: #111111;
      --btn-text: #ffffff;
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, monospace, sans-serif;
    }

    body {
      background-color: var(--bg-color);
      color: var(--text-main);
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 20px;
    }

    .card {
      background: var(--card-bg);
      border: 2px solid var(--border-color);
      width: 100%;
      max-width: 380px;
      padding: 32px 24px;
      box-shadow: 6px 6px 0px var(--border-color);
      text-align: center;
    }

    .title {
      font-size: 1.3rem;
      font-weight: 800;
      letter-spacing: 1px;
      text-transform: uppercase;
      margin-bottom: 12px;
    }

    .subtitle {
      font-size: 0.95rem;
      font-weight: 600;
      color: var(--text-muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
      line-height: 1.4;
      margin-bottom: 32px;
    }

    .actions {
      display: flex;
      flex-direction: column;
      gap: 12px;
    }

    .btn {
      display: block;
      width: 100%;
      padding: 14px 16px;
      font-weight: 700;
      font-size: 0.9rem;
      letter-spacing: 1px;
      text-transform: uppercase;
      text-decoration: none;
      border: 2px solid var(--border-color);
      transition: all 0.2s ease;
      cursor: pointer;
    }

    .btn-primary {
      background-color: var(--btn-bg);
      color: var(--btn-text);
    }

    .btn-secondary {
      background-color: transparent;
      color: var(--text-main);
    }

    .btn:active {
      transform: translate(2px, 2px);
    }
  </style>
</head>
<body>

  <main class="card">
    <h1 class="title">ANDREA GUIJARRO</h1>
    <p class="subtitle">Digital Marketing &<br>Creative Strategy</p>

    <div class="actions">
      <a href="portfolio_andreagm_2026.pdf" target="_blank" class="btn btn-primary">VER PORTFOLIO</a>
      <a href="portfolio_andreagm_2026.pdf" download="Andrea_Guijarro_Portfolio.pdf" class="btn btn-secondary">DESCARGAR PDF</a>
    </div>
  </main>

</body>
</html>